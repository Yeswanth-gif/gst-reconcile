[project]
name = "gst-reconcile"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "selenium>=4.47.0",
    "webdriver-manager>=4.1.2",
]
